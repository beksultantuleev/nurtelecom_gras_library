new version with twine:

$env:HTTP_PROXY = "http://xxx"
$env:HTTPS_PROXY = "http://xxx"

python -m build
twine upload dist/*

git tag -a v2.1.7 -m "Version 2.1.7"
git push --tags


Old versions with setup.py
___________
Change version in setup.py!!

1>  python setup.py sdist
2>  twine upload dist/* 
